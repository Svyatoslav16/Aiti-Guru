# Общее описание

Приложения создано по шаблону vite

Используемые пакеты:
- MUI библиотека компонентов, также для реализации таблицы товаров
- Effector в качестве стейт менеджера
- zod для валидации схема
- react-hook-form для простой реализации формы авторизации, оверинжиниринг, но тут была задача показать, что есть опыт работы с таким

# TODO, будет обновляться по мере реализации
- ~~Окно авторизации~~
- ~~Таблица товаров~~
- Сортировка в таблице
- Диалоговое окно для мокового добавления товара


# Запуск
```npm run dev```

и ```localhost:5173``` (просто стандартный порт vite, можно настроить в vite.config -> server, не было необходимости)

# Upd: По коммитам можно увидеть, что тут не ИИ-шкой сгенерированный код)
